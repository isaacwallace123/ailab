---
all:
  children:
    ai_nodes:
      hosts:
        ai-node-01:
          ansible_host: 192.168.0.210
          ansible_user: isaac
          ansible_ssh_private_key_file: "{{ lookup('env', 'AILAB_SSH_PRIVATE_KEY_PATH') }}"
        ai-node-02:
          ansible_host: 192.168.0.211
          ansible_user: isaac
          ansible_ssh_private_key_file: "{{ lookup('env', 'AILAB_SSH_PRIVATE_KEY_PATH') }}"
